rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow school members to read and write records
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
